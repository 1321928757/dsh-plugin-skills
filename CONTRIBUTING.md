# Contributing

感谢你为 DSH Plugin Skills 提交改进。

## 可以贡献什么

- 修正过时或不准确的 DSH 运行时说明；
- 补充插件开发、UI、README 和发布案例；
- 修正拼写、链接、命令和示例；
- 增加可复用模板或验证脚本；
- 报告与当前 DSH 版本相关的兼容性问题。

## 提交前检查

请确认：

- 每个 skill 目录包含有效的 `SKILL.md`；
- `SKILL.md` 顶部的 `name` 与目录名一致；
- 文档中的路径、命令和链接没有明显错误；
- 没有提交 API key、token、密码、数据库连接信息或本机私有配置；
- 没有把社区实践描述成官方保证；
- 没有直接复制其他项目的受版权保护内容。

## Pull Request 建议

1. 从最新的 `main` 创建分支；
2. 一个 Pull Request 聚焦一个主题；
3. 在描述中说明修改原因和验证方式；
4. 如果更新了 DSH 版本相关内容，请附上对应的源码、运行时 Inspect 或实际验证依据；
5. 保持英文和中文说明的事实一致。

## 内容原则

本项目是非官方社区 skill 集合。涉及 DSH API、Slot、Service、主题令牌和发布平台的说明，应尽量注明适用范围，并避免没有依据的绝对安全或兼容性承诺。
