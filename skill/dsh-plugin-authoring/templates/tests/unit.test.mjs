import test from 'node:test'
import assert from 'node:assert/strict'

// Replace this scaffold with pure behavior tests. It intentionally does not
// claim coverage for Loader, Client, Tool, Remote, or HMR behavior.
test('unit-test scaffold', { skip: 'replace with plugin behavior assertions' }, () => {
  assert.fail('scaffold')
})
